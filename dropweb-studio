#!/usr/bin/env bash
# dropweb-studio — тонкая обёртка: канонический запуск студии живёт в run.sh.
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"
exec "$DIR/run.sh" studio "$@"
